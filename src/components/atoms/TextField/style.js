import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    textInput: {
        textAlign: 'center',
        color: 'white',
        height: 50,
        width: '100%',
        fontSize: 17
    }
});
